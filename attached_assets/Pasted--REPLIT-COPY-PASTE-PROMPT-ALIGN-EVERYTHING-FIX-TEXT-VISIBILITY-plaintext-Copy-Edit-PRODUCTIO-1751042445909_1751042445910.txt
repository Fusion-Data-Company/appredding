💣 REPLIT COPY-PASTE PROMPT — ALIGN EVERYTHING, FIX TEXT VISIBILITY
plaintext
Copy
Edit
🚨 PRODUCTION FIX REQUEST — MULTI-SECTION ALIGNMENT + TEXT VISIBILITY

I need you to fix the following **issues together in one pass**:

---

🧱 ISSUE 1: MISALIGNED SERVICE BOXES ON HOMEPAGE

Component: Home > "Residential Solar", "Commercial Solar", "Battery Storage", "Service & Repair" cards  
Inspect shows absolute gradient overlays like:
<div class="absolute -inset-0.5 bg-gradient-to-br ... blur-lg ...">

🔧 FIX:
- Wrap all 4 cards in a single flex container with:
  ```tsx
  className="flex flex-wrap gap-6 justify-center items-stretch"
Each card must have:

tsx
Copy
Edit
className="relative w-full min-h-[580px] flex flex-col justify-between p-6"
Place the gradient overlay like this inside the card:

tsx
Copy
Edit
<div className="absolute -inset-0.5 bg-gradient-to-br ... blur-lg opacity-20 pointer-events-none z-0"></div>
Wrap all card content in:

tsx
Copy
Edit
<div className="relative z-10">...</div>
📐 GOAL:

All cards equal height

Clean layout flow with no visual jumping

No hover/blur overlays breaking layout

🧠 ISSUE 2: UNREADABLE FORM FIELD PLACEHOLDER & LABEL TEXT

Component: "Utility Savings Calculator"
Circled input examples:

Placeholder: “Enter amount”, “Square footage”

Label: “Monthly Electric Bill ($)”, “Home Size (sq ft)”

Inspect shows:

html
Copy
Edit
<input class="... bg-white/80 ... font-medium" placeholder="Enter amount">
🔧 FIX:

Replace translucent background with:

tsx
Copy
Edit
bg-white
Add Tailwind placeholder override:

tsx
Copy
Edit
placeholder-gray-700 text-gray-900
Apply these to ALL calculator inputs

📥 FINAL CLASS EXAMPLE:

tsx
Copy
Edit
<input
  type="number"
  placeholder="Enter amount"
  className="w-full px-4 py-3 bg-white border-2 border-orange-200 rounded-xl placeholder-gray-700 text-gray-900 focus:border-orange-400 focus:ring-4 focus:ring-orange-100 transition-all duration-300 font-medium"
/>
Ensure <label> tags above inputs are styled with:

tsx
Copy
Edit
className="text-gray-800 font-semibold text-sm"
✅ FINAL REQUIREMENTS:

All 4 cards perfectly aligned regardless of text length

All input placeholders and labels readable at all times

Layout fully responsive

No broken spacing, margin, or hover/blur conflicts

No need for follow-up fix prompts — make it final

Do not split into phases. Apply all changes in one commit and show me the result.